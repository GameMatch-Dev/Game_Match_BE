# Independent read-only code review

Review the supplied change independently. Do not propose edits outside the stated scope. Return JSON with a `findings` array. Each finding must contain `severity`, `title`, `evidence`, `risk`, and `recommendation`. If no actionable defect is supported, return an empty array.

## Authoritative requirement

{{REQUIREMENT}}

## Current diff

{{DIFF}}

## Relevant context

{{CONTEXT}}

## Executed-test evidence

{{TEST_EVIDENCE}}
